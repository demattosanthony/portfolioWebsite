import React from "react";

function OSDesc() {
  return (
    <>
      Undergraduate Research Project that investigates the use of computer
      vision to locate vacant spaces withing a parking lot then alert a user
      with a mobile app. Specifcally the effectiveness of a convolutional neural
      network to classify the difference between a car and a empty parking
      space, storing the locations in a cloud database, then making the
      information available within an app will be addressed.
      <br />
      <br />
      <h2>Technologies:</h2>
      <br />
      Python, Pytorch, OpenCV, Jupyter Notebook, Flutter/Dart
    </>
  );
}

export default OSDesc;
